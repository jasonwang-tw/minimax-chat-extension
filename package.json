{
  "name": "minimax-chat-extension",
  "version": "1.0.0",
  "description": "MiniMax AI Chat Extension - 串接 MiniMax API 的 Chrome 擴充功能",
  "scripts": {
    "build:css": "npx sass src/styles/sidepanel.scss sidepanel/sidepanel.css && npx sass src/styles/options.scss options/options.css",
    "build": "npm run build:css"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.18",
    "postcss": "^8.4.35",
    "sass": "^1.71.1",
    "tailwindcss": "^3.4.1"
  }
}
